name: TestFlight Continuous Deployment

on:
  push:
    branches:
      - staging # Triggers the build automatically on every single commit to staging

concurrency:
  group: testflight-${{ github.ref }}
  cancel-in-progress: true # Cancels an older build if a new commit is pushed immediately after

jobs:
  build-and-deploy:
    name: TestFlight Upload
    runs-on: macos-15 # Standard GitHub macOS runner environment

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Select Xcode Version
        run: sudo xcode-select -s /Applications/Xcode_26.3.app

      - name: Setup Ruby Environment
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Install Fastlane
        run: |
          gem install bundler
          bundle install
          
        # ADD THIS STEP HERE: This builds and unlocks the keychain before Fastlane looks for it
      - name: Create Temporary Keychain
        run: |
          security create-keychain -p "temporary_runner_password" fastlane_tmp_keychain
          security default-keychain -s fastlane_tmp_keychain
          security unlock-keychain -p "temporary_runner_password" fastlane_tmp_keychain
          security set-keychain-settings -t 3600 -u fastlane_tmp_keychain

      - name: Execute Fastlane Beta Lane
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          
          # CHANGE THIS: Inject the token directly into the target clone URL
          MATCH_GIT_URL: https://x-token-auth:${{ secrets.PERSONAL_ACCESS_TOKEN }}@github.com/DhamodaranSri/ios-certificates-fpil.git
          
          FASTLANE_USER: ${{ secrets.FASTLANE_USER }}
          FASTLANE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${{ secrets.FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD }}
          
          # These point Fastlane to the keychain we just created above
          MATCH_KEYCHAIN_NAME: "fastlane_tmp_keychain"
          MATCH_KEYCHAIN_PASSWORD: "temporary_runner_password"
          FASTLANE_DONT_STORE_PASSWORD_IN_KEYCHAIN: "1"
          
          MATCH_P12_PASSWORD: "12345"
        run: bundle exec fastlane deploy_testflight
